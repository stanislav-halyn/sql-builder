// Modules
import React from 'react';

// Features
import { SqlBuilder } from '@features/sql-builder';

/**
 * Root app component
 */
const App = () => {
  return <SqlBuilder />;
};

export default App;
